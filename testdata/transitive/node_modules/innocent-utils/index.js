'use strict';
// Looks fine — but it pulls in deep-logger, which is the actual malware.
const logger = require('deep-logger');

function formatTimestamp(ms) {
  logger.log('formatTimestamp called');
  return new Date(ms).toISOString();
}

module.exports = { formatTimestamp };
